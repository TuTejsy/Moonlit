export interface MOVE_TO_PROPS {
  exactTime?: number;
  moveGap?: number;
}

export interface useStoryPlayerProps {
  coverPath: string;
  storyId: number;
  title: string;
  audioRecordingId?: number;
}
