import { WINDOW_HEIGHT } from './layout';

import { dw, dh } from '@/utils/sizes';

export const HORIZONTAL_PADDING = dw(16);

export const PURE_TAB_HEIGHT = dw(52);

export const DEFAULT_HEADER_HEIGHT = dw(52);

export const FILTERS_SCREEN_BUTTONS_AREA = dw(66);

export const MAX_MODAL_HEIGHT = WINDOW_HEIGHT - dh(81);

export const MODAL_CONTENT_HEIGHT = 'CONTENT_HEIGHT';
