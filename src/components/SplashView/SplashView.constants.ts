import { WINDOW_HEIGHT } from '@gorhom/bottom-sheet';

import { dw } from '@/utils/sizes';

export const LAUNCH_LOGO_HEIGHT = dw(45);
export const LAUNCH_LOGO_MARGIN_TOP = (WINDOW_HEIGHT - LAUNCH_LOGO_HEIGHT) / 2;

export const STARS_MARGIN_TOP = -dw(48);
