import { SUFFICIENT_WINDOW_WIDTH } from '@/constants/layout';
import { HORIZONTAL_PADDING } from '@/constants/sizes';

export const TAB_WIDTH = (SUFFICIENT_WINDOW_WIDTH - HORIZONTAL_PADDING * 2 - 8) / 2;
