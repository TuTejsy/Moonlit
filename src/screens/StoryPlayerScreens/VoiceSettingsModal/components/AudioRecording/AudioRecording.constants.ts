import { WINDOW_MAX_WIDTH } from '@/constants/layout';
import { dw } from '@/utils/sizes';

export const AUDIO_RECORDING_HEIGHT = dw(184, WINDOW_MAX_WIDTH);
export const AUDIO_RECORDING_WIDTH = dw(163, WINDOW_MAX_WIDTH);
