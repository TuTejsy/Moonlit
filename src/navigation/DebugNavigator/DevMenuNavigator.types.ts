import { GenerateRouteParams } from '../types/utils';

import { DevMenuRoutes } from './DevMenuNavigator.routes';

// eslint-disable-next-line @typescript-eslint/ban-types
export type DevMenuStackParams = GenerateRouteParams<DevMenuRoutes, {}>;
